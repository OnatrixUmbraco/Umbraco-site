@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<Umbraco.Cms.Core.Models.Blocks.BlockListItem>
@using ContentModels = Umbraco.Cms.Web.Common.PublishedModels

@{
    var content = (ContentModels.ServiceCard)Model.Content;
}

<article class="service-card">
    <a class="service-card-link">
        <h4 class="fw-semibold service-card-header">@content.ServiceCardHeader</h4>

        <div class="service-card-media">
            @if (content.ServiceCardImage != null)
            {
                <img src="@content.ServiceCardImage.Url()" alt="Service Card" />
            }
        </div>
        <p class="service-card-text">@content.ServiceCardText</p>
    </a>
</article>